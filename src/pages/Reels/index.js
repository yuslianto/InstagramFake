import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Reels = () => {
  return (
    <View>
      <Text>Reels Page</Text>
    </View>
  );
};

export default Reels;

const styles = StyleSheet.create({});
