import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const MainApp = () => {
  return (
    <View>
      <Text>this is main app page</Text>
    </View>
  );
};

export default MainApp;

const styles = StyleSheet.create({});
